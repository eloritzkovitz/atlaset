import type { Settings } from "@types";

export const defaultSettings: Settings = {
  id: "main",
  homeCountry: "",
  colorHomeCountry: false,
  theme: "dark",
  overlayPalettes: {
    standard: "Default",
    yearly: "Default",
    cumulative: "Default",
  },
};
